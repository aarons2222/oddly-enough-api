{
  "name": "oddly-enough-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.36.1",
    "node-fetch": "^2.7.0"
  }
}
